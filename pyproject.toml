[project]
name = "gpu-transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "orch>=0.0.2",
    "torch>=2.8.0",
    "transformers>=4.55.2",
]
